import { createLocale } from './common.en'

export const preactEn = createLocale({
    footerFramework: 'Preact',
    skillsFramework: 'Preact',
    stackFramework: 'Preact',
})

export type PreactTranslations = typeof preactEn
