import { createLocale } from './common.en'

export const vueEn = createLocale({
    footerFramework: 'Vue 3',
    skillsFramework: 'Vue',
    stackFramework: 'Vue',
})

export type VueTranslations = typeof vueEn
